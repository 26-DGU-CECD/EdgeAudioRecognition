# Raspberry Pi 5 / Raspberry Pi OS 64-bit (Bookworm, Python 3.11)
# D-Bus와 PyGObject는 pip가 아니라 apt 패키지를 사용한다.

torch>=2.4
torchaudio>=2.4
torchvision>=0.19
timm>=1.0.3

tensorflow>=2.16,<3
tensorflow_hub>=0.16
setuptools<81
numpy>=1.26

sounddevice>=0.4.6
soundfile>=0.12
librosa>=0.10

# MPU9250 IMU (I2C). --imu 옵션을 쓸 때만 필요하다.
smbus2>=0.4
